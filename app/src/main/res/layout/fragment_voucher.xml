<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context="id.telkomsel.merchant.ui.pelanggan.beranda.voucher.VoucherFragment">

    <data>
        <variable
            name="viewModel"
            type="id.telkomsel.merchant.ui.pelanggan.beranda.voucher.VoucherViewModel"
            />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        android:theme="@style/CustomTheme"
        snackbar="@{viewModel.message}"
        >
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabs"
            android:layout_width="match_parent"
            android:layout_height="@dimen/_55"
            app:tabSelectedTextColor="@color/colorPrimary"
            app:tabGravity="fill"
            app:tabIconTint="@color/colorPrimary"
            android:layout_alignParentTop="true"
            app:tabTextAppearance="@style/CustomTabText"
            android:paddingBottom="0dp"
            android:paddingTop="0dp"
            android:background="@color/white"
            />
        <include
            android:id="@+id/progress"
            layout="@layout/progress_loading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            visible="@{viewModel.isShowLoading}"
            android:visibility="gone"
            />
        <androidx.viewpager.widget.ViewPager
            android:id="@+id/viewPager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            android:layout_below="@+id/tabs"
            />
    </RelativeLayout>
</layout>
